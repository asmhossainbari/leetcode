class Solution(object):
    def toLowerCase(self, str):
        """
        :type str: str
        :rtype: str
        """
        # str = list(str)
        # for i in range(len(str)):
        #     str[i] = str[i].lower()
        # return "".join(str)

        return str.lower()
